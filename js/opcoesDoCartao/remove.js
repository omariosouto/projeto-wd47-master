// Função que Executa Imediatamente
// IIFE = Imediatly Invocked Function by Expression
(function () {
    "use strict"
    
    // Pegou elemento
    const btns = document.querySelectorAll('.opcoesDoCartao-remove')

    // VAR === Escopo Dinâmico
    // LET === Escopo Léxico ( Contexto de leitura )

    for(let i = 0; i < btns.length; i++) { // JavaScript 6 - ES6 - Es2015
        // console.log('Dentro do for', btns[i])
        // Adiciona evento


    }

})()


// Adicionar essa classe cartao--some